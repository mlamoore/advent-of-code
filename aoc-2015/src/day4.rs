use aoc_runner_derive::{aoc, aoc_generator};


#[aoc(day4, part1)]
pub fn solve_part1(input: &str) -> usize {
    let mut answer = 0;

    answer
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_md5() {
        assert_eq!(5, 5);
    }
}
